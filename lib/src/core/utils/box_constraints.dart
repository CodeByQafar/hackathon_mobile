import 'package:flutter/material.dart';

class BoxConstraintses {
//sign view widgets

static BoxConstraints expandSignButton = BoxConstraints.expand(height: 60,width: 1000);
static BoxConstraints signWithIconButton=BoxConstraints(maxWidth: 270);
}